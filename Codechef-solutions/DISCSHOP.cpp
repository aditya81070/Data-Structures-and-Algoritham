#include<bits/stdc++.h>
using namespace std;

int main() {
  int t;
  cin>>t;
  while(t--) {
    string num;
    int flag = 0, first, sec;
    cin>>num;
    int i = 0;
    while(i<num.length()-1) {
      first = num[i] - '0';
      sec = num[i+1] - '1';
      if(first > sec) {

      }
    }
  }
  return 0;
}