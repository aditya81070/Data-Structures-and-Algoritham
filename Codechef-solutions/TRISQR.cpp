#include<bits/stdc++.h>
using namespace std;

int main() {
  int t;
  cin>>t;
  while(t--) {
    long int base, answer = 0;
    cin>>base;
    while(base > 2) {
      answer += (base - 2) /2;
      base = base -2;
    }
    cout<<b
  }
  return 0;
}